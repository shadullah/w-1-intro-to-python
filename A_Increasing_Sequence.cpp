#include <iostream>
#include <vector>
#include <algorithm>

using namespace std;

int main()
{
    int t;
    cin >> t;

    while (t--)
    {
        int n;
        cin >> n;
        int a[n];
        int b[n];
        for (int i = 0; i < n; i++)
        {
            cin >> a[i];
        }
        int b = 1;
        for (int i = 0; i < n; i++)
        {
            if (a[0] == 1 && b == 1)
                continue;
            else if (a[])
        }
    }

    return 0;
}
